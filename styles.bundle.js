webpackJsonp(["styles"],{

/***/ "./node_modules/raw-loader/index.js!./node_modules/postcss-loader/lib/index.js??embedded!./src/styles.css":
/***/ (function(module, exports) {

module.exports = "body, html {\n    height: 100%;\n    margin: 0;\n}\n@-webkit-keyframes jssorl-004-double-tail-spin {\n    0% {\n        -webkit-transform: rotate(0deg);\n                transform: rotate(0deg)\n    }\n    to {\n        -webkit-transform: rotate(360deg);\n                transform: rotate(360deg)\n    }\n}\n@keyframes jssorl-004-double-tail-spin {\n    0% {\n        -webkit-transform: rotate(0deg);\n                transform: rotate(0deg)\n    }\n    to {\n        -webkit-transform: rotate(360deg);\n                transform: rotate(360deg)\n    }\n}\n@-webkit-keyframes sk-cubeGridScaleDelay {\n    0%,\n    70%,\n    to {\n        -webkit-transform: scale3D(1, 1, 1);\n        transform: scale3D(1, 1, 1)\n    }\n    35% {\n        -webkit-transform: scale3D(0, 0, 1);\n        transform: scale3D(0, 0, 1)\n    }\n}\n@keyframes sk-cubeGridScaleDelay {\n    0%,\n    70%,\n    to {\n        -webkit-transform: scale3D(1, 1, 1);\n        transform: scale3D(1, 1, 1)\n    }\n    35% {\n        -webkit-transform: scale3D(0, 0, 1);\n        transform: scale3D(0, 0, 1)\n    }\n}\n/* @font-face {\n    font-family: 'aktiv_groteskbold';\n    src: url(../fonts/aktivgrotesk_std_bd-webfont.eot);\n    src: url(../fonts/aktivgrotesk_std_bd-webfont.eot?#iefix) format('embedded-opentype'), url(../fonts/aktivgrotesk_std_bd-webfont.woff2) format('woff2'), url(../fonts/aktivgrotesk_std_bd-webfont.woff) format('woff'), url(../fonts/aktivgrotesk_std_bd-webfont.ttf) format('truetype'), url(../fonts/aktivgrotesk_std_bd-webfont.svg#aktiv_groteskbold) format('svg');\n    font-weight: 400;\n    font-style: normal\n}\n\n@font-face {\n    font-family: 'aktiv_groteskitalic';\n    src: url(../fonts/aktivgrotesk_std_it-webfont.eot);\n    src: url(../fonts/aktivgrotesk_std_it-webfont.eot?#iefix) format('embedded-opentype'), url(../fonts/aktivgrotesk_std_it-webfont.woff2) format('woff2'), url(../fonts/aktivgrotesk_std_it-webfont.woff) format('woff'), url(../fonts/aktivgrotesk_std_it-webfont.ttf) format('truetype'), url(../fonts/aktivgrotesk_std_it-webfont.svg#aktiv_groteskitalic) format('svg');\n    font-weight: 400;\n    font-style: normal\n}\n\n@font-face {\n    font-family: 'aktiv_grotesklight';\n    src: url(../fonts/aktivgrotesk_std_lt-webfont.eot);\n    src: url(../fonts/aktivgrotesk_std_lt-webfont.eot?#iefix) format('embedded-opentype'), url(../fonts/aktivgrotesk_std_lt-webfont.woff2) format('woff2'), url(../fonts/aktivgrotesk_std_lt-webfont.woff) format('woff'), url(../fonts/aktivgrotesk_std_lt-webfont.ttf) format('truetype'), url(../fonts/aktivgrotesk_std_lt-webfont.svg#aktiv_grotesklight) format('svg');\n    font-weight: 400;\n    font-style: normal\n}\n\n@font-face {\n    font-family: 'aktiv_groteskregular';\n    src: url(../fonts/aktivgrotesk_std_rg-webfont.eot);\n    src: url(../fonts/aktivgrotesk_std_rg-webfont.eot?#iefix) format('embedded-opentype'), url(../fonts/aktivgrotesk_std_rg-webfont.woff2) format('woff2'), url(../fonts/aktivgrotesk_std_rg-webfont.woff) format('woff'), url(../fonts/aktivgrotesk_std_rg-webfont.ttf) format('truetype'), url(../fonts/aktivgrotesk_std_rg-webfont.svg#aktiv_groteskregular) format('svg');\n    font-weight: 400;\n    font-style: normal\n}\n\n@font-face {\n    font-family: 'aktiv_groteskxbold';\n    src: url(../fonts/aktivgrotesk_std_xbd-webfont.eot);\n    src: url(../assets/fontsf/aktivgrotesk_std_xbd-webfont.eot?#iefix) format('embedded-opentype'), url(../fonts/aktivgrotesk_std_xbd-webfont.woff2) format('woff2'), url(../fonts/aktivgrotesk_std_xbd-webfont.woff) format('woff'), url(../fonts/aktivgrotesk_std_xbd-webfont.ttf) format('truetype'), url(../fonts/aktivgrotesk_std_xbd-webfont.svg#aktiv_groteskxbold) format('svg');\n    font-weight: 400;\n    font-style: normal\n}\n\n@font-face {\n    font-family: 'gothambold1';\n    src: url(../fonts/gothambold1.eot);\n    src: local('../fonts/gothambold1'), url(../fonts/gothambold1.woff) format('woff'), url(../fonts/gothambold1.ttf) format('truetype')\n}\n */\n.jssorl-004-double-tail-spin img {\n    -webkit-animation-name: jssorl-004-double-tail-spin;\n            animation-name: jssorl-004-double-tail-spin;\n    -webkit-animation-duration: 11.2s;\n            animation-duration: 11.2s;\n    -webkit-animation-iteration-count: infinite;\n            animation-iteration-count: infinite;\n    -webkit-animation-timing-function: linear;\n            animation-timing-function: linear\n}\n#numbers {\n    padding-left: 15px;\n    font-family: 'aktiv_groteskregular', Arial, sans-serif\n}\n#numbers,\n.about-txt p {\n    font-size: 16px\n}\n.activities-back {\n    background: rgba(255, 255, 255, .73);\n    padding: 20px\n}\nh4.subtitles {\n    color: #677078;\n    text-align: left;\n    line-height: 1.4\n}\n.icons {\n    padding: 1px\n}\nbody {\n    color: #8d8d8d;\n    font-family: 'aktiv_groteskbold', Arial, sans-serif;\n    line-height: 1;\n    padding: 0\n}\nbody.boxed {\n    max-width: 1170px;\n    background: #f4f5f8\n}\na {\n    color: #a6ce39;\n    text-decoration: none;\n    -webkit-transition: .5s ease all;\n    transition: .5s ease all\n}\na:active,\na:focus,\na:hover {\n    text-decoration: none;\n    color: #000\n}\n.container {\n    max-width: 100%!important;\n    width: 1170px!important\n}\np.icons {\n    color: #41454f!important\n}\n.wrapper {\n    overflow: hidden;\n    min-height: 700px\n}\n.flexslider .slides>li,\n.wrapper,\nheader {\n    position: relative\n}\nimg {\n    max-width: 100%\n}\n.styled-list .list-unstyled ol,\nul {\n    list-style-type: none\n}\nol,\nul {\n    margin: 0;\n    padding: 0\n}\nol li,\nul li {\n    color: #8d8d8d;\n    line-height: 1.8\n}\nol.list-unstyled {\n    margin-left: 0\n}\n.list-unstyled li i {\n    padding-right: 15px;\n    color: #ffda43\n}\nh1,\nh2,\nh3,\nh4,\nh5,\nh6 {\n    font-family: 'aktiv_groteskbold', Arial, sans-serif;\n    color: #2a2a2a\n}\np {\n    font-size: 16px;\n    color: #677078;\n    line-height: 1.688;\n    margin-bottom: 20px;\n    text-align: justify;\n    font-family: 'aktiv_groteskregular'\n}\np+p:last-child {\n    margin-bottom: 0\n}\n.btn,\nblockquote {\n    border: 0;\n    -webkit-transition: .5s ease all;\n    transition: .5s ease all\n}\nblockquote {\n    background: #ffda44;\n    color: #675f3f;\n    text-align: left;\n    margin: 0;\n    font-style: italic;\n    padding: 30px 35px;\n    font-size: 20px;\n    line-height: 1.455\n}\n.light {\n    font-weight: 100\n}\n.btn {\n    border-radius: 3px;\n    font-size: 16px;\n    font-family: 'aktiv_groteskbold', Arial, sans-serif;\n    font-weight: 700;\n    line-height: 48px;\n    padding: 0 22px;\n    text-transform: uppercase\n}\n.btn-primary,\n.btn-primary:active,\n.btn-primary:active:hover,\n.btn-primary:focus {\n    background: #a6ce39;\n    color: #fff\n}\n.btn-primary:hover,\nbutton.btn.btn-warning:hover {\n    background: rgba(166, 206, 57, .7)\n}\n.btn-default {\n    background: #ffda44;\n    color: #4c443a\n}\n.btn-default:hover {\n    background: #ecc124\n}\n.btn-projects {\n    font-size: 17px;\n    color: #61656f;\n    padding: 24px 30px;\n    display: block;\n    line-height: 1;\n    border-bottom: 1px solid #fcfcfd;\n    background: 0 0;\n    border-top: none;\n    border-left: none;\n    border-right: none;\n    width: 100%;\n    text-align: left\n}\n.btn-projects:focus {\n    outline: 0\n}\n.btn-projects:active {\n    -webkit-box-shadow: none;\n            box-shadow: none;\n    outline: 0\n}\n.btn-projects-active {\n    font-size: 17px;\n    color: #fff;\n    padding: 24px 30px;\n    display: block;\n    line-height: 1;\n    border-bottom: 1px solid #fcfcfd;\n    background: #a6ce39;\n    border-top: none;\n    border-left: none;\n    border-right: none;\n    width: 100%;\n    text-align: left\n}\n.btn-projects-active:focus {\n    outline: 0\n}\n.btn-projects-active:active {\n    -webkit-box-shadow: none;\n            box-shadow: none;\n    outline: 0\n}\nsection {\n    padding: 40px 0\n}\n.sidebar {\n    padding: 40px 13px\n}\nmain.main {\n    padding-bottom: 40px\n}\n.spacer-10,\n.spacer-100,\n.spacer-20,\n.spacer-30,\n.spacer-40,\n.spacer-5,\n.spacer-50,\n.spacer-60,\n.spacer-70,\n.spacer-80,\n.spacer-90 {\n    display: block;\n    clear: both;\n    overflow: hidden\n}\n.spacer-5 {\n    height: 5px\n}\n.spacer-10 {\n    height: 10px\n}\n.spacer-20 {\n    height: 20px\n}\n.spacer-30 {\n    height: 30px\n}\n.spacer-40 {\n    height: 40px\n}\n.spacer-50 {\n    height: 50px\n}\n.spacer-60 {\n    height: 60px\n}\n.spacer-70 {\n    height: 70px\n}\n.spacer-80 {\n    height: 80px\n}\n.spacer-90 {\n    height: 90px\n}\n.spacer-100 {\n    height: 100px\n}\n@media all and (max-width:991px) {\n    section {\n        padding: 40px 0\n    }\n}\n@media all and (max-width:767px) {\n    br {\n        display: none\n    }\n}\n@media all and (max-width:480px) {\n    section {\n        padding: 30px 0\n    }\n}\n.cd-top {\n    display: inline-block;\n    height: 36px;\n    width: 36px;\n    position: fixed;\n    bottom: 40px;\n    right: 10px;\n    -webkit-box-shadow: 0 0 10px rgba(0, 0, 0, .05);\n            box-shadow: 0 0 10px rgba(0, 0, 0, .05);\n    overflow: hidden;\n    text-indent: 100%;\n    white-space: nowrap;\n    background: rgba(166, 206, 57, .7) url(\"data:image/svg+xml,%3C%3Fxml version%3D%221.0%22 encoding%3D%22utf-8%22%3F%3E%3C!-- Generator%3A Adobe Illustrator 17.1.0%2C SVG Export Plug-In . SVG Version%3A 6.00 Build 0)  --%3E%3C!DOCTYPE svg PUBLIC %22-%2F%2FW3C%2F%2FDTD SVG 1.1%2F%2FEN%22 %22http%3A%2F%2Fwww.w3.org%2FGraphics%2FSVG%2F1.1%2FDTD%2Fsvg11.dtd%22%3E%3Csvg version%3D%221.1%22 id%3D%22Layer_1%22 xmlns%3D%22http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%22 xmlns%3Axlink%3D%22http%3A%2F%2Fwww.w3.org%2F1999%2Fxlink%22 x%3D%220px%22 y%3D%220px%22%09 width%3D%2216px%22 height%3D%2216px%22 viewBox%3D%220 0 16 16%22 enable-background%3D%22new 0 0 16 16%22 xml%3Aspace%3D%22preserve%22%3E%3Cpolygon fill%3D%22%23FFFFFF%22 points%3D%228%2C2.8 16%2C10.7 13.6%2C13.1 8.1%2C7.6 2.5%2C13.2 0%2C10.7 %22%2F%3E%3C%2Fsvg%3E\") no-repeat center 50%;\n    visibility: hidden;\n    opacity: 0;\n    -webkit-transition: opacity .3s 0s, visibility 0s .3s;\n    transition: opacity .3s 0s, visibility 0s .3s\n}\n.cd-top:hover {\n    background-color: #a6ce39\n}\n.cd-top.cd-is-visible {\n    visibility: visible;\n    opacity: 1\n}\n@media only screen and (min-width:767px) {\n    .cd-top {\n        right: 20px;\n        bottom: 20px\n    }\n}\n@media only screen and (min-width:1024px) {\n    .cd-top {\n        height: 40px;\n        width: 40px;\n        right: 30px;\n        bottom: 30px\n    }\n}\n.bold, .heading, .top-info p, p#boldunerline {\n    font-family: 'aktiv_groteskbold';\n}\n.breadcrumbs a:hover, ul.main-menu li a:hover, ul.main-menu li.active>a {\n    color: #a6ce39;\n}\n.slick-dots {\n    position: absolute;\n    bottom: 0;\n    left: 0;\n    margin: 0 auto;\n    right: 0;\n    display: inline-block;\n    text-align: center;\n    bottom: -14px;\n}\n.slick-dots > li {\n    display: inline-block;\n}\n.slick-dots > li button, .slick-dots > li button:hover {\n    color: transparent;\n    background: transparent !important;\n    width: 23px;\n    height: 10px;\n    padding: 0;\n    border: 3px solid #435151 !important;\n    border-radius: 32% !important;\n    margin: 0 4px;\n    outline: 0;\n}\n.slick-dots > li.slick-active button {\n    background: #435151 !important;\n}\n\n\n"

/***/ }),

/***/ "./node_modules/style-loader/lib/addStyles.js":
/***/ (function(module, exports, __webpack_require__) {

/*
	MIT License http://www.opensource.org/licenses/mit-license.php
	Author Tobias Koppers @sokra
*/

var stylesInDom = {};

var	memoize = function (fn) {
	var memo;

	return function () {
		if (typeof memo === "undefined") memo = fn.apply(this, arguments);
		return memo;
	};
};

var isOldIE = memoize(function () {
	// Test for IE <= 9 as proposed by Browserhacks
	// @see http://browserhacks.com/#hack-e71d8692f65334173fee715c222cb805
	// Tests for existence of standard globals is to allow style-loader
	// to operate correctly into non-standard environments
	// @see https://github.com/webpack-contrib/style-loader/issues/177
	return window && document && document.all && !window.atob;
});

var getElement = (function (fn) {
	var memo = {};

	return function(selector) {
		if (typeof memo[selector] === "undefined") {
			var styleTarget = fn.call(this, selector);
			// Special case to return head of iframe instead of iframe itself
			if (styleTarget instanceof window.HTMLIFrameElement) {
				try {
					// This will throw an exception if access to iframe is blocked
					// due to cross-origin restrictions
					styleTarget = styleTarget.contentDocument.head;
				} catch(e) {
					styleTarget = null;
				}
			}
			memo[selector] = styleTarget;
		}
		return memo[selector]
	};
})(function (target) {
	return document.querySelector(target)
});

var singleton = null;
var	singletonCounter = 0;
var	stylesInsertedAtTop = [];

var	fixUrls = __webpack_require__("./node_modules/style-loader/lib/urls.js");

module.exports = function(list, options) {
	if (typeof DEBUG !== "undefined" && DEBUG) {
		if (typeof document !== "object") throw new Error("The style-loader cannot be used in a non-browser environment");
	}

	options = options || {};

	options.attrs = typeof options.attrs === "object" ? options.attrs : {};

	// Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
	// tags it will allow on a page
	if (!options.singleton && typeof options.singleton !== "boolean") options.singleton = isOldIE();

	// By default, add <style> tags to the <head> element
	if (!options.insertInto) options.insertInto = "head";

	// By default, add <style> tags to the bottom of the target
	if (!options.insertAt) options.insertAt = "bottom";

	var styles = listToStyles(list, options);

	addStylesToDom(styles, options);

	return function update (newList) {
		var mayRemove = [];

		for (var i = 0; i < styles.length; i++) {
			var item = styles[i];
			var domStyle = stylesInDom[item.id];

			domStyle.refs--;
			mayRemove.push(domStyle);
		}

		if(newList) {
			var newStyles = listToStyles(newList, options);
			addStylesToDom(newStyles, options);
		}

		for (var i = 0; i < mayRemove.length; i++) {
			var domStyle = mayRemove[i];

			if(domStyle.refs === 0) {
				for (var j = 0; j < domStyle.parts.length; j++) domStyle.parts[j]();

				delete stylesInDom[domStyle.id];
			}
		}
	};
};

function addStylesToDom (styles, options) {
	for (var i = 0; i < styles.length; i++) {
		var item = styles[i];
		var domStyle = stylesInDom[item.id];

		if(domStyle) {
			domStyle.refs++;

			for(var j = 0; j < domStyle.parts.length; j++) {
				domStyle.parts[j](item.parts[j]);
			}

			for(; j < item.parts.length; j++) {
				domStyle.parts.push(addStyle(item.parts[j], options));
			}
		} else {
			var parts = [];

			for(var j = 0; j < item.parts.length; j++) {
				parts.push(addStyle(item.parts[j], options));
			}

			stylesInDom[item.id] = {id: item.id, refs: 1, parts: parts};
		}
	}
}

function listToStyles (list, options) {
	var styles = [];
	var newStyles = {};

	for (var i = 0; i < list.length; i++) {
		var item = list[i];
		var id = options.base ? item[0] + options.base : item[0];
		var css = item[1];
		var media = item[2];
		var sourceMap = item[3];
		var part = {css: css, media: media, sourceMap: sourceMap};

		if(!newStyles[id]) styles.push(newStyles[id] = {id: id, parts: [part]});
		else newStyles[id].parts.push(part);
	}

	return styles;
}

function insertStyleElement (options, style) {
	var target = getElement(options.insertInto)

	if (!target) {
		throw new Error("Couldn't find a style target. This probably means that the value for the 'insertInto' parameter is invalid.");
	}

	var lastStyleElementInsertedAtTop = stylesInsertedAtTop[stylesInsertedAtTop.length - 1];

	if (options.insertAt === "top") {
		if (!lastStyleElementInsertedAtTop) {
			target.insertBefore(style, target.firstChild);
		} else if (lastStyleElementInsertedAtTop.nextSibling) {
			target.insertBefore(style, lastStyleElementInsertedAtTop.nextSibling);
		} else {
			target.appendChild(style);
		}
		stylesInsertedAtTop.push(style);
	} else if (options.insertAt === "bottom") {
		target.appendChild(style);
	} else if (typeof options.insertAt === "object" && options.insertAt.before) {
		var nextSibling = getElement(options.insertInto + " " + options.insertAt.before);
		target.insertBefore(style, nextSibling);
	} else {
		throw new Error("[Style Loader]\n\n Invalid value for parameter 'insertAt' ('options.insertAt') found.\n Must be 'top', 'bottom', or Object.\n (https://github.com/webpack-contrib/style-loader#insertat)\n");
	}
}

function removeStyleElement (style) {
	if (style.parentNode === null) return false;
	style.parentNode.removeChild(style);

	var idx = stylesInsertedAtTop.indexOf(style);
	if(idx >= 0) {
		stylesInsertedAtTop.splice(idx, 1);
	}
}

function createStyleElement (options) {
	var style = document.createElement("style");

	options.attrs.type = "text/css";

	addAttrs(style, options.attrs);
	insertStyleElement(options, style);

	return style;
}

function createLinkElement (options) {
	var link = document.createElement("link");

	options.attrs.type = "text/css";
	options.attrs.rel = "stylesheet";

	addAttrs(link, options.attrs);
	insertStyleElement(options, link);

	return link;
}

function addAttrs (el, attrs) {
	Object.keys(attrs).forEach(function (key) {
		el.setAttribute(key, attrs[key]);
	});
}

function addStyle (obj, options) {
	var style, update, remove, result;

	// If a transform function was defined, run it on the css
	if (options.transform && obj.css) {
	    result = options.transform(obj.css);

	    if (result) {
	    	// If transform returns a value, use that instead of the original css.
	    	// This allows running runtime transformations on the css.
	    	obj.css = result;
	    } else {
	    	// If the transform function returns a falsy value, don't add this css.
	    	// This allows conditional loading of css
	    	return function() {
	    		// noop
	    	};
	    }
	}

	if (options.singleton) {
		var styleIndex = singletonCounter++;

		style = singleton || (singleton = createStyleElement(options));

		update = applyToSingletonTag.bind(null, style, styleIndex, false);
		remove = applyToSingletonTag.bind(null, style, styleIndex, true);

	} else if (
		obj.sourceMap &&
		typeof URL === "function" &&
		typeof URL.createObjectURL === "function" &&
		typeof URL.revokeObjectURL === "function" &&
		typeof Blob === "function" &&
		typeof btoa === "function"
	) {
		style = createLinkElement(options);
		update = updateLink.bind(null, style, options);
		remove = function () {
			removeStyleElement(style);

			if(style.href) URL.revokeObjectURL(style.href);
		};
	} else {
		style = createStyleElement(options);
		update = applyToTag.bind(null, style);
		remove = function () {
			removeStyleElement(style);
		};
	}

	update(obj);

	return function updateStyle (newObj) {
		if (newObj) {
			if (
				newObj.css === obj.css &&
				newObj.media === obj.media &&
				newObj.sourceMap === obj.sourceMap
			) {
				return;
			}

			update(obj = newObj);
		} else {
			remove();
		}
	};
}

var replaceText = (function () {
	var textStore = [];

	return function (index, replacement) {
		textStore[index] = replacement;

		return textStore.filter(Boolean).join('\n');
	};
})();

function applyToSingletonTag (style, index, remove, obj) {
	var css = remove ? "" : obj.css;

	if (style.styleSheet) {
		style.styleSheet.cssText = replaceText(index, css);
	} else {
		var cssNode = document.createTextNode(css);
		var childNodes = style.childNodes;

		if (childNodes[index]) style.removeChild(childNodes[index]);

		if (childNodes.length) {
			style.insertBefore(cssNode, childNodes[index]);
		} else {
			style.appendChild(cssNode);
		}
	}
}

function applyToTag (style, obj) {
	var css = obj.css;
	var media = obj.media;

	if(media) {
		style.setAttribute("media", media)
	}

	if(style.styleSheet) {
		style.styleSheet.cssText = css;
	} else {
		while(style.firstChild) {
			style.removeChild(style.firstChild);
		}

		style.appendChild(document.createTextNode(css));
	}
}

function updateLink (link, options, obj) {
	var css = obj.css;
	var sourceMap = obj.sourceMap;

	/*
		If convertToAbsoluteUrls isn't defined, but sourcemaps are enabled
		and there is no publicPath defined then lets turn convertToAbsoluteUrls
		on by default.  Otherwise default to the convertToAbsoluteUrls option
		directly
	*/
	var autoFixUrls = options.convertToAbsoluteUrls === undefined && sourceMap;

	if (options.convertToAbsoluteUrls || autoFixUrls) {
		css = fixUrls(css);
	}

	if (sourceMap) {
		// http://stackoverflow.com/a/26603875
		css += "\n/*# sourceMappingURL=data:application/json;base64," + btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))) + " */";
	}

	var blob = new Blob([css], { type: "text/css" });

	var oldSrc = link.href;

	link.href = URL.createObjectURL(blob);

	if(oldSrc) URL.revokeObjectURL(oldSrc);
}


/***/ }),

/***/ "./node_modules/style-loader/lib/urls.js":
/***/ (function(module, exports) {


/**
 * When source maps are enabled, `style-loader` uses a link element with a data-uri to
 * embed the css on the page. This breaks all relative urls because now they are relative to a
 * bundle instead of the current page.
 *
 * One solution is to only use full urls, but that may be impossible.
 *
 * Instead, this function "fixes" the relative urls to be absolute according to the current page location.
 *
 * A rudimentary test suite is located at `test/fixUrls.js` and can be run via the `npm test` command.
 *
 */

module.exports = function (css) {
  // get current location
  var location = typeof window !== "undefined" && window.location;

  if (!location) {
    throw new Error("fixUrls requires window.location");
  }

	// blank or null?
	if (!css || typeof css !== "string") {
	  return css;
  }

  var baseUrl = location.protocol + "//" + location.host;
  var currentDir = baseUrl + location.pathname.replace(/\/[^\/]*$/, "/");

	// convert each url(...)
	/*
	This regular expression is just a way to recursively match brackets within
	a string.

	 /url\s*\(  = Match on the word "url" with any whitespace after it and then a parens
	   (  = Start a capturing group
	     (?:  = Start a non-capturing group
	         [^)(]  = Match anything that isn't a parentheses
	         |  = OR
	         \(  = Match a start parentheses
	             (?:  = Start another non-capturing groups
	                 [^)(]+  = Match anything that isn't a parentheses
	                 |  = OR
	                 \(  = Match a start parentheses
	                     [^)(]*  = Match anything that isn't a parentheses
	                 \)  = Match a end parentheses
	             )  = End Group
              *\) = Match anything and then a close parens
          )  = Close non-capturing group
          *  = Match anything
       )  = Close capturing group
	 \)  = Match a close parens

	 /gi  = Get all matches, not the first.  Be case insensitive.
	 */
	var fixedCss = css.replace(/url\s*\(((?:[^)(]|\((?:[^)(]+|\([^)(]*\))*\))*)\)/gi, function(fullMatch, origUrl) {
		// strip quotes (if they exist)
		var unquotedOrigUrl = origUrl
			.trim()
			.replace(/^"(.*)"$/, function(o, $1){ return $1; })
			.replace(/^'(.*)'$/, function(o, $1){ return $1; });

		// already a full url? no change
		if (/^(#|data:|http:\/\/|https:\/\/|file:\/\/\/)/i.test(unquotedOrigUrl)) {
		  return fullMatch;
		}

		// convert the url to a full url
		var newUrl;

		if (unquotedOrigUrl.indexOf("//") === 0) {
		  	//TODO: should we add protocol?
			newUrl = unquotedOrigUrl;
		} else if (unquotedOrigUrl.indexOf("/") === 0) {
			// path should be relative to the base url
			newUrl = baseUrl + unquotedOrigUrl; // already starts with '/'
		} else {
			// path should be relative to current directory
			newUrl = currentDir + unquotedOrigUrl.replace(/^\.\//, ""); // Strip leading './'
		}

		// send back the fixed url(...)
		return "url(" + JSON.stringify(newUrl) + ")";
	});

	// send back the fixed css
	return fixedCss;
};


/***/ }),

/***/ "./src/styles.css":
/***/ (function(module, exports, __webpack_require__) {

// style-loader: Adds some css to the DOM by adding a <style> tag

// load the styles
var content = __webpack_require__("./node_modules/raw-loader/index.js!./node_modules/postcss-loader/lib/index.js??embedded!./src/styles.css");
if(typeof content === 'string') content = [[module.i, content, '']];
// Prepare cssTransformation
var transform;

var options = {"hmr":true}
options.transform = transform
// add the styles to the DOM
var update = __webpack_require__("./node_modules/style-loader/lib/addStyles.js")(content, options);
if(content.locals) module.exports = content.locals;
// Hot Module Replacement
if(false) {
	// When the styles change, update the <style> tags
	if(!content.locals) {
		module.hot.accept("!!../node_modules/raw-loader/index.js!../node_modules/postcss-loader/lib/index.js??embedded!./styles.css", function() {
			var newContent = require("!!../node_modules/raw-loader/index.js!../node_modules/postcss-loader/lib/index.js??embedded!./styles.css");
			if(typeof newContent === 'string') newContent = [[module.id, newContent, '']];
			update(newContent);
		});
	}
	// When the module is disposed, remove the <style> tags
	module.hot.dispose(function() { update(); });
}

/***/ }),

/***/ 2:
/***/ (function(module, exports, __webpack_require__) {

module.exports = __webpack_require__("./src/styles.css");


/***/ })

},[2]);
//# sourceMappingURL=styles.bundle.js.map